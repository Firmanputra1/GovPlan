import React from 'react';
import StyledHeader from '../components/StyledHeader';

const Header = () => {
    return (
        <StyledHeader>
            <h1>Logo</h1>
            {/* Isi header */}
        </StyledHeader>
    );
}

export default Header;
